# orbital-signature-mfa
Instead of a static password (which can be intercepted), a satellite requires a "Kinetic Signature." To authorize a high-risk command (like "De-orbit"), the controller must send a sequence of micro-thruster bursts. The satellite uses ML to verify if the "rhythm" and "force" of these bursts match the authorized pilot's behavioral profile.
